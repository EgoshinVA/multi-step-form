import {createSlice} from "@reduxjs/toolkit";

const initialState = {

}

const step1Steps = createSlice({
    name: 'step1',
    initialState,
    reducers: create => ({})
})

export default step1Steps.reducer;